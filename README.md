# forge

Shared foundational Go libraries.

## Packages

- `llm`: provider clients, prompt loading, structured extraction, orchestration runtime, and reusable prompt runners.

## Scope

`forge` is for cross-project infrastructure, not product/domain logic.
Project-specific config loading, prompt directories, prompt sets, and business logic stay in consuming repos.
